import { Component } from '@angular/core';

/**
 * Stub login component — replaced by the full OIDC flow in Phase 5+.
 * Kept minimal so the AppShell route is functional during local dev.
 */
@Component({
  selector: 'gg-login',
  standalone: true,
  template: `<p>Sign in flow lives here.</p>`,
})
export class LoginComponent {}
